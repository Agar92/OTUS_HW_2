#include <algorithm>
#include <vector>

using std::string;
using std::vector;

// Custom Comparator to sort the array in increasing order
bool customComparator(string, string);

// Function to sort the IP Addresses
vector<string> sortIPAddress(vector<string>);
